# Careax 프로젝트 제품 요구사항 문서 (PRD) - v3 (강사 운영 최적화 & 매출 연산형)

## 1. 개요 (Overview)
* **프로젝트명:** Careax 1인 강사 운영 최적화 통합 데이터 및 매출 통계 시스템 구축
* **배경 및 목적:** 다수의 강의를 운영하는 강사가 홈페이지 수강신청, 문의, 외부 구글폼 등 파편화된 채널의 데이터를 'Google Spreadsheet' 하나로 통합 관리하게 함으로써 관리 효율을 극대화한다. 동시에, 과제 미션에 따라 백엔드(Supabase DB)에서 수강 과목별 금액을 자동 매핑하여 누적 수강 금액 및 실시간 예상 매출 통계를 산출하는 시스템을 하이브리드로 구축한다.
* **개발 스택:** Google Anti-gravity 프레임워크 (Frontend), Google Sheets API (통합 조회 레이어), Supabase DB (누적 금액 및 매출 연산 레이어), Gmail API (알림 레이어).

## 2. 결제 구현 방침 (Payments Policy)
* **현황 및 제약:** 현재 사업자 번호가 없으므로 실제 라이브 카드/계좌 결제 연동은 불가능함.
* **해결안 (과제 스펙):** 포트원(PortOne) 또는 토스페이먼츠의 **'테스트용 샌드박스 상점 ID(MID)'**를 사용하여 가상의 카드 결제 UI 및 프로세스를 모의 구현하고, 결제 완료 이벤트 발생 시 데이터 적재 및 금액 매핑 트리거를 실행함.

---

## 3. 핵심 기능 요구사항 (Functional Requirements)

### 3.1. 강사 중심형 데이터 단일화 및 하이브리드 적재 (Dual-Pipeline)
홈페이지에서 수강신청(`Enroll / Register`) 또는 문의하기(`Inquiry`)가 발생하면 시스템은 데이터를 이원화하지 않고 다음 두 채널로 동시에 동기화한다.

1. **[운영 채널] Google Spreadsheet (통합 조회용)**
   * 기존 외부 Google Form 응답이 쌓이던 **동일한 구글 스프레드시트 파일의 지정 시트(또는 탭)**로 실시간 추가(`Append`).
   * 강사가 외부 일정 중에도 스마트폰 구글 시트 앱 하나로 모든 수강생 현황을 누락 없이 파악할 수 있도록 함.
2. **[연산 채널] Supabase DB (미션 과제 및 매출 연산용)**
   * `enrollments` 테이블에 신청자 정보와 매핑된 강의 금액을 누적 적재.

### 3.2. 수강 과목 금액 매핑 및 예상 매출 통계 (Supabase)
* **강의 가격 마스터 테이블 (`courses`) 구성:**
  * 각 강의 코드별 고정 금액 정의 (예: `A 강의` = 150,000원 / `B 강의` = 200,000원).
* **예상 매출 누적 로직:**
  * 사용자가 특정 강의를 신청하면 DB 트랜잭션 내에서 `courses` 테이블의 가격을 참조하여 `enrollments.price` 필드에 자동으로 매핑 및 저장함.
  * `SELECT SUM(price) FROM enrollments` 쿼리를 수행하여 현재까지 접수된 **전체 누적 수강 금액(총 예상 매출)**을 실시간으로 연산 및 누적함.

### 3.3. 강사 실시간 알림 시스템 (careax.rana@gmail.com)
* **트리거:** 홈페이지 신청 및 문의 제출 완료 시 강사 메일로 즉시 알림 발송.
* **제목 규칙 (Naming Convention) 확립:** 강사가 이동 중 메일 제목만 보고 직관적으로 매출과 인입 건을 파악할 수 있도록 고정 대괄호와 가격 정보를 명시함.
  * 수강신청 시: `[Careax-수강신청] 홍길동님이 'A 강의'를 신청했습니다. (예상매출 +150,000원)`
  * 문의등록 시: `[Careax-문의하기] 이몽룡님의 새로운 문의사항이 접수되었습니다.`
* **시각적 부채 제공 (조건부 서식):**
  * 구글 스프레드시트 내에 `처리 상태` 열을 두고, 기본값은 '대기(Pending)'로 지정.
  * 강사가 '완료'로 바꾸기 전까지는 해당 데이터 행 전체가 **옅은 주황색/노란색 배경색으로 자동 하이라이트**되어 미처리 건 유실을 원천 차단함.

### 3.4. 학생 대상 자동 응답 이메일 시스템
* **트리거:** 수강신청 등록 완료 또는 문의 접수 완료 즉시.
* **수신처:** 신청자가 폼에 입력한 개인 이메일 주소.
* **이메일 본문 필수 반영 문안:**
  > "*** 강의 신청해 주셔서 감사합니다. 확인하고 메일로 다시 연락드리겠습니다."
* **디자인 및 스타일:** Careax 홈페이지의 브랜드 가이드라인(메인 식별 컬러, 타이포그래피 규칙, 상단 로고 레이아웃)을 완벽히 적용한 일관된 HTML 이메일 템플릿 사용.

---

## 4. 변경 내용 기록 (Revision History)
* **v3.0 (최종수정):** 
  * Google Anti-gravity 및 Supabase DB 하이브리드 아키텍처 반영.
  * 강사 실무 환경을 고려하여 모든 인입 데이터를 **구글 스프레드시트 단일 채널로 통합**하여 조회 편의성 확보.
  * 사업자 등록 부재에 따른 **테스트 결제 샌드박스** 도입 가이드 추가.
  * Supabase 내 가격 매핑 테이블 구축 및 누적 예상 매출 통계 연산 요구사항 반영.